# Mini Jarvis appliance shutdown rule
# Allows only the minijarvis user to run the dedicated shutdown helper without a password.
# Do not replace this with broad NOPASSWD permissions.

minijarvis ALL=(root) NOPASSWD: /usr/local/sbin/mini-jarvis-shutdown
